import React from 'react';
import { render } from 'react-dom';
import './index.scss';

class LotteryForm extends React.Component {
  render() {
    return null;
  }
};

render(<LotteryForm />, document.getElementById('root'));
